<?phpnamespace admin;use Move\db;class lendone extends admin{	public function __construct()	{		parent::__construct();		if (!$this->is_game('lendone')) {			ajax('这个应用你没有购买');		}	}	public function index()	{		if (empty($_POST)) {			$data['list'] = md('sys_config')->lists();			$builder = new builder();			$builder->data($data)->url()->title('特殊理财', '这里是特殊理财')->edit();		} else {			if (APP_DEMO) {				ajax('测试站暂时不能修改');			}		}	}}