<?phpnamespace admin;use Move\db;class xissue extends admin{	public function __construct()	{		parent::__construct();		if (!$this->is_game('xissue')) {			ajax('这个应用你没有购买');		}	}	public function index()	{		if (empty($_POST)) {			$data['list'] = md('sys_config')->lists();			$builder = new builder();			$builder->data($data)->url()->title('特殊认购', '这里是特殊认购')->edit();		} else {			if (APP_DEMO) {				ajax('测试站暂时不能修改');			}		}	}}