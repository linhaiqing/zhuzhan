<?phpnamespace admin;use Move\db;class news extends admin{	public function __construct()	{		parent::__construct();		if (!$this->is_game('news')) {			ajax('这个应用你没有购买');		}	}	public function index()	{		if (empty($_POST)) {			$data['list'] = md('sys_config')->lists();			$builder = new builder();			$builder->data($data)->url()->title('新闻配置', '这里是新闻配置')->edit();		} else {			if (APP_DEMO) {				ajax('测试站暂时不能修改');			}		}	}}